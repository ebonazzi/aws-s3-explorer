# Normalise all text files to LF in the repo.
# Git will convert to CRLF on Windows checkout (core.autocrlf) if configured,
# but the canonical form stored in the repo is always LF.
* text=auto eol=lf

# Explicitly mark binary files so Git never converts them.
*.png  binary
*.jpg  binary
*.ico  binary
